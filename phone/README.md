# Aether phone blotter

PAPER TRADING ONLY. Read-only WebView of the PC desk at port 8791.

The token lives in `../ledger/phone_token.txt` (not in git). `assets/bootstrap.json` is generated on the PC at build time and is gitignored.

Away from home: log Tailscale in on the PC and the A16, then put the Tailscale MagicDNS / IP in the app settings gear.
