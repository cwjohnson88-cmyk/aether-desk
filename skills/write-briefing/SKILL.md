---
name: write-briefing
description: Write Aether daily briefing and 12–18 line phone card from scan + book + hypotheses.
---

# Write briefing

Run `python -m aether brief` when the PC is available. Otherwise assemble from `data/snapshots/scan.json`, `ledger/paper_account.json`, and `ledger/hypotheses.json`.

Required sections (see `bots/briefing_editor.md`). Max 3 hypotheses, each with falsifier. Phone card 12–18 lines. PAPER ONLY on the first line. Overseer must review before publish. Do not invent movers to fill space.
